
    mod = __import__("datetime")
    func = getattr(getattr(mod, "datetime"), "now")

    while True:
            taskTextText = self.toText(self.listen())
            if taskTextText == "start listening":
                counter = 0
                while counter != 3:
                    dispatcher = Dispatcher(self.toText(self.listen()))
                    if dispatcher.dispatch() == "unsuccessful":
                        counter += 1
                    elif dispatcher.dispatch() == "close":
                        sys.exit()